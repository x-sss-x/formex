{
  "$schema": "https://turbo.build/schema.json",
  "extends":["//"],
  "tasks": {
    "dev": {
      "cache": false
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**"]
    },
    "clean": {
      "dependsOn": ["^clean"],
      "cache": false
    }
  }
}
{
    "name": "@formex/api",
    "private":"true",
    "scripts": {
        "dev":"composer run dev",
        "clean":"rm -rf node_modules vendor .cache",
        "composer:install": "composer install"
    }
}
